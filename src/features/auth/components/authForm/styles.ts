import styled from 'styled-components'

export const FormWrapper = styled.form`
  width: 900px;
  background-color: #fff;
  box-shadow: 4px 7px 25px -10px rgba(252, 204, 43, 1);
  padding: 20px;
  border-radius: 10px;

  .auth-form-input {
    margin-bottom: 20px;

    &:last-child {
      margin-bottom: 0;
    }
  }
`
